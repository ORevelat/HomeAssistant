weather:
  - platform: darksky
    api_key: !secret darksky_api_key
    mode: daily

sensor:
  - platform: darksky
    api_key: !secret darksky_api_key
    language: fr
    monitored_conditions:
      - summary
      - temperature
    scan_interval:
      minutes: 15
