
recorder:
  purge_interval: 1
  purge_keep_days: 7
  db_url: !secret recorder_db_url
  include:
    domains:
      - input_boolean
      - input_number
      - input_select
    entities:
      - sensor.auribeau_sur_siagne_temperature
